export interface Issue {
        id:string,
        issueTitle: string,
        issueAssignedTo: string,
        issueDescription: string,
        issueStatus: string,
        issueDate: any,
        imagePath: string,
        creator: string,
        reporter:string
}